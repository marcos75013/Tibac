name: petit_bac
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  go_router: ^5.0.0
  cupertino_icons: ^1.0.6
  confetti: ^0.7.0  # Update to the latest version
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1  # Update to the latest version

flutter:
  uses-material-design: true
  assets:
    - lib/assets/pngs/
    - lib/assets/jpgs/
    - lib/assets/sounds/bell_sound.mp3

flutter_icons:
  android: true
  ios: true
  image_path: "lib/assets/pngs/petit_bac_logo.png"
